# dude — a replicated authenticated log. See ../SPEC.md.
#
# Deliberately empty of re-exports: import modules directly (`from dude import codec`).
# The previous package's root grew a stale module list and a stale doc revision that
# outlived every fact in it; there is nothing here to go stale.
